package {{PACKAGE}};

import jakarta.persistence.*;
import lombok.Data;

@Entity
@Table(name = "{{TABLE_NAME}}")
@Data
public class {{ENTITY_NAME}}Entity {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    // TODO: Add JPA columns
}
